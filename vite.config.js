import { defineConfig } from 'vite';

export default defineConfig({
  // Set to '/florescence/' when deployed to GitHub Pages at
  // https://USERNAME.github.io/florescence/
  // Change to '/' if you're deploying to a custom domain root.
  base: '/florescence/',
});
